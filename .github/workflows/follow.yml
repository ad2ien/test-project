name: Follow contributor

on:
  pull_request:
    branches: [main]

  workflow_dispatch:
    
jobs:
  follow-user:
    runs-on: ubuntu-20.04
    steps:
      - name: Follow user
        uses: okp4/follow-contributor-action@v1.0.0
        with:
          username: hleonardtek
          token: ${{ secrets.FOLLOW_TOKEN }}
